name: .NET

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
      name: Checkout Code
     
    #- name: Setup MSBuild Path
    #  uses: warrenbuckley/Setup-MSBuild@v1
       
    - name: Setup NuGet
      uses: NuGet/setup-nuget@v1.0.5
     
    - name: Restore NuGet Packages
      run: nuget restore SharedSitecore.Extensions.TranslateExtensions.TextSetting.sln
 
    #- name: Build and Publish Web App
    #  run: msbuild SharedSitecore.Extensions.TranslateExtensions.TextSetting.sln /p:Configuration=Release /p:DeployOnBuild=true
 
    - name: package publish nuget
      id: publish_nuget
      uses: rohith/publish-nuget@v2
      with:
          # Filepath of the project to be packaged, relative to root of repository
          PROJECT_FILE_PATH: SharedSitecore.Extensions.TranslateExtensions.TextSetting/SharedSitecore.Extensions.TranslateExtensions.TextSetting/src/SharedSitecore.Extensions.TranslateExtenisons.TextSetting.csproj
